version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: canchauser
      POSTGRES_PASSWORD: canchapass
      POSTGRES_DB: canchadb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: